# NNI — Coming Soon (notes)

This short companion file was added by the scaffolding script and explains the created assets for the coming soon page.

Files added by the scaffolder:

- `index.html` — Landing page
- `styles.css` — Styles
- `script.js` — Countdown + subscribe (front-end only)

Open `index.html` in your browser to preview the page. To persist subscribers, replace the `saveEmail` function in `script.js` with your backend endpoint.
